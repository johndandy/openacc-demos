#ifndef SYNC_H
#define SYNC_H

#include <stdio.h>

int* gen_array(int*, size_t, int, int);
int* init_zero(int*, size_t);
int print_array(int*, size_t);
int wait_demo(void);
int atom_demo(void);

#endif // SYNC_H
